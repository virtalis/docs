---
title: GUI
summary: Functions for interacting with the Visionary Render GUI. 
---

# GUI

Functions for interacting with the Visionary Render GUI.

## Functions

| Type           | Name           |
| -------------- | -------------- |
| void | **[vrAddToolbarButton](#vraddtoolbarbutton)**([vrnode](../classes/vrnode.mdx) toolbarNode, [vrnode](../classes/vrnode.mdx) groupNode, string name, number type, string caption, string icon, string command, string userString, string clusterScope, string statusTxt)<br />Adds a button to a toolbar dock group.  |
| [vrnode](../classes/vrnode.mdx) | **[vrAddToolbarButtonGroup](#vraddtoolbarbuttongroup)**([vrnode](../classes/vrnode.mdx) toolbarUINode, string groupName, boolean hasScrollBar =true)<br />Adds a new group for toolbar buttons to be attached to.  |
| [vrnode](../classes/vrnode.mdx) | **[vrAddToolbarDock](#vraddtoolbardock)**([vrnode](../classes/vrnode.mdx) notebookNode, string tabName, string tabIcon)<br />Adds a docked window to a notebook that toolbars can be added to.  |
| void | **[vrCloseNotebookPane](#vrclosenotebookpane)**(string notebookName, string paneName)<br />Closes the specified pane of the given notebook.  |
| void | **[vrCloseSequence](#vrclosesequence)**([vrnode](../classes/vrnode.mdx) selection)<br />Closes the specified sequence in the sequencer window.  |
| void | **[vrFilterView](#vrfilterview)**(string view, string metaType)<br />Sets the filter for a specified view.  |
| void | **[vrGalleryRequestImport](#vrgalleryrequestimport)**(string typeName, string importerName, string prompt, [vrnode](../classes/vrnode.mdx) destination =nil)<br />Imports a file using an importer registered with the gallery.  |
| void | **[vrHideModalDialogues](#vrhidemodaldialogues)**(boolean suppressed =false)<br />Suppresses all modal dialogue message boxes.  |
| void | **[vrLoadGalleryFolders](#vrloadgalleryfolders)**(string path)<br />Add sub-folders of the specified folder to the gallery.  |
| string | **[vrMessageBox](#vrmessagebox)**(string text, string caption, string flagList)<br />Displays a message box to the user.  |
| void | **[vrOpenNotebookPane](#vropennotebookpane)**(string notebookName, string paneName)<br />Opens the specified pane of the given notebook.  |
| void | **[vrPopupGalleryWindow](#vrpopupgallerywindow)**()<br />Pops up the gallery window.  |
| void | **[vrPopupPropertyWindow](#vrpopuppropertywindow)**([vrnode](../classes/vrnode.mdx) view =nil)<br />Pops open the property window.  |
| void | **[vrPopupToolsWindow](#vrpopuptoolswindow)**()<br />Pops up the tools window.  |
| void | **[vrPresentMessage](#vrpresentmessage)**(string msg)<br />Presents a message box to the user.  |
| void | **[vrProgressBegin](#vrprogressbegin)**(string/int(optional) msg/ val, int(optional) max, string(optional) msg)<br />Begins showing the progress display.  |
| void | **[vrProgressEnd](#vrprogressend)**()<br />Ends the progress display.  |
| void | **[vrProgressEvents](#vrprogressevents)**(boolean enabled =true)<br />Create Nodes under user tree to indicate when progress events are occuring.  |
| void | **[vrProgressYield](#vrprogressyield)**(string/int(optional) msg/ val, int(optional) max, string(optional) msg)<br />Updates the progress display.  |
| void | **[vrResetWindows](#vrresetwindows)**()<br />Resets the windows to their initial positions and states that they were when the application was started.  |
| void | **[vrSetGUIEnabled](#vrsetguienabled)**(boolean enabled, [vrnode](../classes/vrnode.mdx) userNode =nil)<br />Shows or hides the windows over the 3D scene.  |
| void | **[vrSetIconToCurrentView](#vrseticontocurrentview)**([vrnode](../classes/vrnode.mdx) node)<br />Sets the browser icon for a node to the current view.  |
| void | **[vrSetIconToDefault](#vrseticontodefault)**([vrnode](../classes/vrnode.mdx) node)<br />Sets the browser icon for a node to the default icon.  |
| void | **[vrSetIconToTexture](#vrseticontotexture)**([vrnode](../classes/vrnode.mdx) node, [vrnode](../classes/vrnode.mdx) texture)<br />Sets the browser icon for a node to a texture.  |
| void | **[vrSetStatusBar](#vrsetstatusbar)**(string text, number duration =0.0)<br />Set the text and duration of the text in the status bar.  |
| void | **[vrShowAnnotationReviewWindow](#vrshowannotationreviewwindow)**([vrnode](../classes/vrnode.mdx) annotationNode =nil)<br />Pops up the applicaiton annotation review window for the specified annotation node.  |
| void | **[vrShowGalleryType](#vrshowgallerytype)**(string typeName)<br />Shows a type in the gallery window.  |
| void | **[vrShowGradientWindow](#vrshowgradientwindow)**([vrnode](../classes/vrnode.mdx) gradientNode)<br />Pops up the application gradient editor for the specified gradient node.  |
| void | **[vrShowGUIFrame](#vrshowguiframe)**([vrnode](../classes/vrnode.mdx) uiNode, boolean show)<br />Show or hide a GUI frame.  |
| void | **[vrShowImporterSettings](#vrshowimportersettings)**([vrnode](../classes/vrnode.mdx) importerNode)<br />Invokes a dialog that shows the importer settings for the given node.  |
| void | **[vrShowNextSelection](#vrshownextselection)**(number distance =1)<br />Navigates to the next selection and shows the properties of the nodes.  |
| void | **[vrShowNodeProperties](#vrshownodeproperties)**([vrnode](../classes/vrnode.mdx) selection, boolean popup =false)<br />Pops up the application property editor for the specified view selection.  |
| void | **[vrShowPreviousSelection](#vrshowpreviousselection)**(number distance =1)<br />Navigates to the previous selection and shows the properties of the nodes.  |
| void | **[vrShowPropertyWindow](#vrshowpropertywindow)**([vrnode](../classes/vrnode.mdx) selection =nil, boolean popup =false)<br />Pops up the application property editor for the specified view selection.  |
| void | **[vrShowScriptWindow](#vrshowscriptwindow)**([vrnode](../classes/vrnode.mdx) scriptNode =nil)<br />Pops up the application script editor for the specified script node.  |
| void | **[vrShowSequence](#vrshowsequence)**([vrnode](../classes/vrnode.mdx) selection)<br />Shows the specified sequence in the sequencer window.  |
| void | **[vrShowSequencer](#vrshowsequencer)**([vrnode](../classes/vrnode.mdx) selection)<br />Shows the sequencer window.  |
| void | **[vrShowSettingsWindow](#vrshowsettingswindow)**([vrnode](../classes/vrnode.mdx) selection =nil, number tabIndex =-1)<br />Pops up the application settings for the specified settings group.  |
| void | **[vrToggleGUIEnabled](#vrtoggleguienabled)**([vrnode](../classes/vrnode.mdx) userNode =nil)<br />Toggles the visibility of the windows over the 3D scene.  |
| boolean | **[vrToggleNotebookPane](#vrtogglenotebookpane)**(string notebookName, string paneName)<br />If the current pane is visible it will be hidden, if it's hidden it will be made visible (and selected).  |
| void | **[vrToggleSubtreeStats](#vrtogglesubtreestats)**()<br />Toggles the subtree stats display in the properties window.  |
| void | **[vrUpdateMenu](#vrupdatemenu)**([vrnode](../classes/vrnode.mdx) uinode)<br />Create menu item frames for children of the menu node.  |
| void | **[vrUpdateSelectionHistoryMenu](#vrupdateselectionhistorymenu)**([vrnode](../classes/vrnode.mdx) menu)<br />Updates a menu to display the current selection history.  |

## Functions Documentation

### vrAddToolbarButton

```lua
void vrAddToolbarButton(
    vrnode toolbarNode,
    vrnode groupNode,
    string name,
    number type,
    string caption,
    string icon,
    string command,
    string userString,
    string clusterScope,
    string statusTxt
)
```

Adds a button to a toolbar dock group. 

**Parameters**: 

  * **toolbarNode** The toolbar dock to add the button to. 
  * **groupNode** The toolbar dock group to add the button to. 
  * **name** The name of the button node. 
  * **type** The button type. 
  * **caption** The button caption. 
  * **icon** The icon for the button. 
  * **command** The lua command for the button. 
  * **userString** The user parameters for the lua command. 
  * **clusterScope** The cluster scope of the button. 
  * **statusTxt** The status text displayed while hovering over the button 

### vrAddToolbarButtonGroup

```lua
vrnode vrAddToolbarButtonGroup(
    vrnode toolbarUINode,
    string groupName,
    boolean hasScrollBar =true
)
```

Adds a new group for toolbar buttons to be attached to. 

**Parameters**: 

  * **toolbarUINode** The tool tab node to attach the button group to 
  * **groupName** The name of the button group 
  * **hasScrollBar** Whether to allow the group to scroll 

**Return**: the group node 

### vrAddToolbarDock

```lua
vrnode vrAddToolbarDock(
    vrnode notebookNode,
    string tabName,
    string tabIcon
)
```

Adds a docked window to a notebook that toolbars can be added to. 

**Parameters**: 

  * **notebookNode** The notebook uinode to dock to 
  * **tabName** The title of the frame 
  * **tabIcon** The icon of the frame 

**Return**: the dock node 

### vrCloseNotebookPane

```lua
void vrCloseNotebookPane(
    string notebookName,
    string paneName
)
```

Closes the specified pane of the given notebook. 

**Parameters**: 

  * **notebookName** Specifies the name of the notebook. 
  * **paneName** Specifies the name of the pane. 

### vrCloseSequence

```lua
void vrCloseSequence(
    vrnode selection
)
```

Closes the specified sequence in the sequencer window. 

### vrFilterView

```lua
void vrFilterView(
    string view,
    string metaType
)
```

Sets the filter for a specified view. 

**Parameters**: 

  * **view** The name of the view 
  * **metaType** The type to filter 

### vrGalleryRequestImport

```lua
void vrGalleryRequestImport(
    string typeName,
    string importerName,
    string prompt,
    vrnode destination =nil
)
```

Imports a file using an importer registered with the gallery. 

**Parameters**: 

  * **typeName** Specifies the name of a gallery asset type. 
  * **importerName** Specifies the name of a registered importer, or an empty string to use all importers. 
  * **prompt** A prompt to display in the file dialogue. 
  * **destination** The parent node for the imported data. 

### vrHideModalDialogues

```lua
void vrHideModalDialogues(
    boolean suppressed =false
)
```

Suppresses all modal dialogue message boxes. 

**Parameters**: 

  * **suppressed** Whether to suppress the dialogue messages or not 

### vrLoadGalleryFolders

```lua
void vrLoadGalleryFolders(
    string path
)
```

Add sub-folders of the specified folder to the gallery. 

**Parameters**: 

  * **path** Specifies the path to the folder to search. 

### vrMessageBox

```lua
string vrMessageBox(
    string text,
    string caption,
    string flagList
)
```

Displays a message box to the user. 

**Parameters**: 

  * **text** The main body of the message. 
  * **caption** The text to put in the title bar. 
  * **flagList** String indicating what kind of input is required, and an icon to show.

* "okcancel,warning" would display a box with OK and CANCEL buttons, with a warning icon.
* "yesno,question" would display a box with YES and NO buttons, with a question icon.
* "abortretryignore,error" would display ABORT, RETRY and IGNORE buttons, with an error icon. 

**Return**: Returns one of the following based on selected button: cancel, continue, no, ok, retry, tryagain, yes 

### vrOpenNotebookPane

```lua
void vrOpenNotebookPane(
    string notebookName,
    string paneName
)
```

Opens the specified pane of the given notebook. 

**Parameters**: 

  * **notebookName** Specifies the name of the notebook. 
  * **paneName** Specifies the name of the pane. 

### vrPopupGalleryWindow

```lua
void vrPopupGalleryWindow()
```

Pops up the gallery window. 

### vrPopupPropertyWindow

```lua
void vrPopupPropertyWindow(
    vrnode view =nil
)
```

Pops open the property window. 

**Parameters**: 

  * **view** Selection to display the properties of 

### vrPopupToolsWindow

```lua
void vrPopupToolsWindow()
```

Pops up the tools window. 

### vrPresentMessage

```lua
void vrPresentMessage(
    string msg
)
```

Presents a message box to the user. 

**Parameters**: 

  * **msg** The message to display 

### vrProgressBegin

```lua
void vrProgressBegin(
    string/int(optional) msg/ val,
    int(optional) max,
    string(optional) msg
)
```

Begins showing the progress display. 

**Parameters**: 

  * **msg/val** 

* When the only argument, string message to display
* When not the only argument, integer progress value for bar display 
  * **max** The maximum range of the progress bar 
  * **msg** The message to display 

### vrProgressEnd

```lua
void vrProgressEnd()
```

Ends the progress display. 

### vrProgressEvents

```lua
void vrProgressEvents(
    boolean enabled =true
)
```

Create Nodes under user tree to indicate when progress events are occuring. 

**Parameters**: 

  * **enabled** Whether to enable progress messages 

### vrProgressYield

```lua
void vrProgressYield(
    string/int(optional) msg/ val,
    int(optional) max,
    string(optional) msg
)
```

Updates the progress display. 

**Parameters**: 

  * **msg/val** 

* When the only argument, string message to display
* When not the only argument, integer progress value for bar display 
  * **max** The maximum range of the progress bar 
  * **msg** The message to display 

### vrResetWindows

```lua
void vrResetWindows()
```

Resets the windows to their initial positions and states that they were when the application was started. 

### vrSetGUIEnabled

```lua
void vrSetGUIEnabled(
    boolean enabled,
    vrnode userNode =nil
)
```

Shows or hides the windows over the 3D scene. 

**Parameters**: 

  * **userNode** user whose gui should be affected (defaults to local user) 

### vrSetIconToCurrentView

```lua
void vrSetIconToCurrentView(
    vrnode node
)
```

Sets the browser icon for a node to the current view. 

### vrSetIconToDefault

```lua
void vrSetIconToDefault(
    vrnode node
)
```

Sets the browser icon for a node to the default icon. 

### vrSetIconToTexture

```lua
void vrSetIconToTexture(
    vrnode node,
    vrnode texture
)
```

Sets the browser icon for a node to a texture. 

**Parameters**: 

  * **texture** The texture node 

### vrSetStatusBar

```lua
void vrSetStatusBar(
    string text,
    number duration =0.0
)
```

Set the text and duration of the text in the status bar. 

**Parameters**: 

  * **text** Text to display 
  * **duration** Number of seconds to display the status message 

### vrShowAnnotationReviewWindow

```lua
void vrShowAnnotationReviewWindow(
    vrnode annotationNode =nil
)
```

Pops up the applicaiton annotation review window for the specified annotation node. 

**Parameters**: 

  * **annotationNode** The annotation node to show 

### vrShowGalleryType

```lua
void vrShowGalleryType(
    string typeName
)
```

Shows a type in the gallery window. 

**Parameters**: 

  * **typeName** Specifies the name of the type to show. 

### vrShowGradientWindow

```lua
void vrShowGradientWindow(
    vrnode gradientNode
)
```

Pops up the application gradient editor for the specified gradient node. 

**Parameters**: 

  * **gradientNode** The gradient node to show 

### vrShowGUIFrame

```lua
void vrShowGUIFrame(
    vrnode uiNode,
    boolean show
)
```

Show or hide a GUI frame. 

**Parameters**: 

  * **uiNode** The frame node 
  * **show** True to show, false to hide 

### vrShowImporterSettings

```lua
void vrShowImporterSettings(
    vrnode importerNode
)
```

Invokes a dialog that shows the importer settings for the given node. 

**Parameters**: 

  * **importerNode** Importer node 

### vrShowNextSelection

```lua
void vrShowNextSelection(
    number distance =1
)
```

Navigates to the next selection and shows the properties of the nodes. 

**Parameters**: 

  * **distance** The number of steps forward in the selection history to go 

### vrShowNodeProperties

```lua
void vrShowNodeProperties(
    vrnode selection,
    boolean popup =false
)
```

Pops up the application property editor for the specified view selection. 

### vrShowPreviousSelection

```lua
void vrShowPreviousSelection(
    number distance =1
)
```

Navigates to the previous selection and shows the properties of the nodes. 

**Parameters**: 

  * **distance** The number of steps back in the selection history to go 

### vrShowPropertyWindow

```lua
void vrShowPropertyWindow(
    vrnode selection =nil,
    boolean popup =false
)
```

Pops up the application property editor for the specified view selection. 

**Parameters**: 

  * **selection** The selection group to show properties for 
  * **popup** True if the properties window should be shown if currently hidden 

### vrShowScriptWindow

```lua
void vrShowScriptWindow(
    vrnode scriptNode =nil
)
```

Pops up the application script editor for the specified script node. 

**Parameters**: 

  * **scriptNode** The script node to show 

### vrShowSequence

```lua
void vrShowSequence(
    vrnode selection
)
```

Shows the specified sequence in the sequencer window. 

### vrShowSequencer

```lua
void vrShowSequencer(
    vrnode selection
)
```

Shows the sequencer window. 

**Parameters**: 

  * **selection** The sequence node to show 

### vrShowSettingsWindow

```lua
void vrShowSettingsWindow(
    vrnode selection =nil,
    number tabIndex =-1
)
```

Pops up the application settings for the specified settings group. 

**Parameters**: 

  * **selection** The setting category node to show 
  * **tabIndex** Zero-indexed tab number to show 

### vrToggleGUIEnabled

```lua
void vrToggleGUIEnabled(
    vrnode userNode =nil
)
```

Toggles the visibility of the windows over the 3D scene. 

**Parameters**: 

  * **userNode** user whose gui should be affected (defaults to local user) 

### vrToggleNotebookPane

```lua
boolean vrToggleNotebookPane(
    string notebookName,
    string paneName
)
```

If the current pane is visible it will be hidden, if it's hidden it will be made visible (and selected). 

**Parameters**: 

  * **notebookName** Specifies the name of the notebook. 
  * **paneName** Specifies the name of the pane. 

**Return**: true if the pane is set to visible 

### vrToggleSubtreeStats

```lua
void vrToggleSubtreeStats()
```

Toggles the subtree stats display in the properties window. 

### vrUpdateMenu

```lua
void vrUpdateMenu(
    vrnode uinode
)
```

Create menu item frames for children of the menu node. 

**Parameters**: 

  * **uinode** The menu node parent 

### vrUpdateSelectionHistoryMenu

```lua
void vrUpdateSelectionHistoryMenu(
    vrnode menu
)
```

Updates a menu to display the current selection history. 
