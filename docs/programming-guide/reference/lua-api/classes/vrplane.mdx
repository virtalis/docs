---
title: vrplane
summary: A plane. 
---

# vrplane

**Module:** **[Maths](../modules/maths.mdx)**

A plane. 

`#include <lua_api.h>`

## Public Functions

| Type           | Name           |
| -------------- | -------------- |
| Any | **[copy](#copy)**()<br />(vrCopy) Returns a copy of a Vector, Matrix, Ray, Sphere, AABBox, Quaternion or Plane  |
| number | **[getDistance](#getdistance)**(vrvec3](../classes/vrvec3.mdx) point)<br />(vrPlaneGetPointDistance) Returns the distance a point is from the plane. A negative value means that the point is behind the plane. A positive value means the point is in front. A zero means it is on the plane.  |
| boolean | **[lineIntersect](#lineintersect)**(vrvec3](../classes/vrvec3.mdx) point1, [vrvec3](../classes/vrvec3.mdx) point2)<br />(vrPlaneLineIntersect) Returns whether or not a line made of two points will intersect a plane.  |
| Any | **[operator=](#operator)**() |
| number | **[rayIntersect](#rayintersect)**(vrray](../classes/vrray.mdx) ray)<br />(vrPlaneRayIntersect) Returns how far along the ray an intersection point with this plane is.  |
| [vrplane](../classes/vrplane.mdx) | **[transform](#transform)**(vrmatrix4](../classes/vrmatrix4.mdx) mat)<br />(vrPlaneTransform) Returns a plane transformed by a matrix.  |

## Public Attributes

| Type           | Name           |
| -------------- | -------------- |
| number | **[distance](#distance)** <br />Dot product of any point on the plane with its normal.  |
| [vrvec3](../classes/vrvec3.mdx) | **[normal](#normal)** <br />Normal of the plane.  |

## Public Functions Documentation

### copy

```lua
Any copy()
```

(vrCopy) Returns a copy of a Vector, Matrix, Ray, Sphere, AABBox, Quaternion or Plane 

### getDistance

```lua
number getDistance(
    vrvec3 point
)
```

(vrPlaneGetPointDistance) Returns the distance a point is from the plane. A negative value means that the point is behind the plane. A positive value means the point is in front. A zero means it is on the plane. 

### lineIntersect

```lua
boolean lineIntersect(
    vrvec3 point1,
    vrvec3 point2
)
```

(vrPlaneLineIntersect) Returns whether or not a line made of two points will intersect a plane. 

**Return**: true if the line intersects the plane 

### operator=

```lua
Any operator=()
```

### rayIntersect

```lua
number rayIntersect(
    vrray ray
)
```

(vrPlaneRayIntersect) Returns how far along the ray an intersection point with this plane is. 

### transform

```lua
vrplane transform(
    vrmatrix4 mat
)
```

(vrPlaneTransform) Returns a plane transformed by a matrix. 

## Public Attributes Documentation

### distance

```lua
number distance;
```

Dot product of any point on the plane with its normal. 

### normal

```lua
vrvec3 normal;
```

Normal of the plane. 
