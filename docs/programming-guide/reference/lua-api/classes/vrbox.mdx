---
title: vrbox
summary: A box. 
---

# vrbox

**Module:** **[Maths](../modules/maths.mdx)**

A box. 

`#include <lua_api.h>`

## Public Functions

| Type           | Name           |
| -------------- | -------------- |
| boolean | **[contains](#contains)**(Any b)<br />(vrTestContain) Returns whether or object A completly contains object B.  |
| Any | **[copy](#copy)**()<br />(vrCopy) Returns a copy of a Vector, Matrix, Ray, Sphere, AABBox, Quaternion or Plane  |
| number | **[getExtent](#getextent)**()<br />(vrAABoxExtent) Returns the largest dimension of an axis aligned bounding box.  |
| boolean | **[intersects](#intersects)**(Any b)<br />(vrTestIntersection) Returns whether or not two objects are intersecting one another.  |
| [vrbox](../classes/vrbox.mdx) | **[merge](#merge)**(vrbox](../classes/vrbox.mdx) r)<br />(vrAABoxMerge) Returns an axis aligned bounding box that contains two other bounding boxes.  |
| Any | **[operator=](#operator)**() |
| [vrbox](../classes/vrbox.mdx) | **[transform](#transform)**(vrmatrix4](../classes/vrmatrix4.mdx) matrix)<br />(vrAABoxTransform) Returns an axis aligned bounding box transformed by a matrix.  |

## Public Attributes

| Type           | Name           |
| -------------- | -------------- |
| [vrvec3](../classes/vrvec3.mdx) | **[center](#center)** <br />Centre of the box.  |
| [vrvec3](../classes/vrvec3.mdx) | **[centre](#centre)** <br />Centre of the box.  |
| [vrvec3](../classes/vrvec3.mdx) | **[dimensions](#dimensions)** <br />Dimensions of the box.  |
| [vrvec3](../classes/vrvec3.mdx) | **[max](#max)** <br />Maximum in each axis of the box.  |
| [vrvec3](../classes/vrvec3.mdx) | **[min](#min)** <br />Minimum in each axis of the box.  |

## Public Functions Documentation

### contains

```lua
boolean contains(
    Any b
)
```

(vrTestContain) Returns whether or object A completly contains object B. 

**Parameters**: 

  * **b** A sphere, AABB, or vec3 

**Return**: true if b is inside a 

### copy

```lua
Any copy()
```

(vrCopy) Returns a copy of a Vector, Matrix, Ray, Sphere, AABBox, Quaternion or Plane 

### getExtent

```lua
number getExtent()
```

(vrAABoxExtent) Returns the largest dimension of an axis aligned bounding box. 

### intersects

```lua
boolean intersects(
    Any b
)
```

(vrTestIntersection) Returns whether or not two objects are intersecting one another. 

**Parameters**: 

  * **b** A sphere or AABB 

**Return**: True if the objects are intersecting 

### merge

```lua
vrbox merge(
    vrbox r
)
```

(vrAABoxMerge) Returns an axis aligned bounding box that contains two other bounding boxes. 

### operator=

```lua
Any operator=()
```

### transform

```lua
vrbox transform(
    vrmatrix4 matrix
)
```

(vrAABoxTransform) Returns an axis aligned bounding box transformed by a matrix. 

## Public Attributes Documentation

### center

```lua
vrvec3 center;
```

Centre of the box. 

### centre

```lua
vrvec3 centre;
```

Centre of the box. 

### dimensions

```lua
vrvec3 dimensions;
```

Dimensions of the box. 

### max

```lua
vrvec3 max;
```

Maximum in each axis of the box. 

### min

```lua
vrvec3 min;
```

Minimum in each axis of the box. 
