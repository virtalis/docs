---
title: Types
summary: Defines the data types used for object handle parameters to API functions. 
---

# Types

Defines the data types used for object handle parameters to API functions. 

## Classes

| Type           | Name           |
| -------------- | -------------- |
| struct | **[_TextureInfo](../Classes/struct___texture_info.mdx)**  |

## Types

| Type           | Name           |
| -------------- | -------------- |
| typedef struct SecurityContextHandle * | **[HAuth](../definitions/group__api__types.mdx#typedef-hauth)** <br />A handle to a security context.  |
| typedef struct FFIVarHandle * | **[HFFIVar](../definitions/group__api__types.mdx#typedef-hffivar)** <br />A handle to a variable passed through the foreign function interface (FFI)  |
| typedef struct MetaHandle * | **[HMeta](../definitions/group__api__types.mdx#typedef-hmeta)** <br />A handle to a VRTree metanode.  |
| typedef struct MigrationHandle * | **[HMigration](../definitions/group__api__types.mdx#typedef-hmigration)** <br />A handle to a migration description.  |
| typedef struct NodeHandle * | **[HNode](../definitions/group__api__types.mdx#typedef-hnode)** <br />A handle to a VRTree node.  |
| typedef struct UserDataHandle * | **[HUserSlot](../definitions/group__api__types.mdx#typedef-huserslot)** <br />A handle to a userdata slot on a VRTree node.  |
| typedef struct _TextureInfo | **[TextureInfo](../definitions/group__api__types.mdx#typedef-textureinfo)**  |

## Types Documentation

### typedef HAuth

```cpp
typedef struct SecurityContextHandle* HAuth;
```

A handle to a security context. 

### typedef HFFIVar

```cpp
typedef struct FFIVarHandle* HFFIVar;
```

A handle to a variable passed through the foreign function interface (FFI) 

### typedef HMeta

```cpp
typedef struct MetaHandle* HMeta;
```

A handle to a VRTree metanode. 

### typedef HMigration

```cpp
typedef struct MigrationHandle* HMigration;
```

A handle to a migration description. 

### typedef HNode

```cpp
typedef struct NodeHandle* HNode;
```

A handle to a VRTree node. 

### typedef HUserSlot

```cpp
typedef struct UserDataHandle* HUserSlot;
```

A handle to a userdata slot on a VRTree node. 

### typedef TextureInfo

```cpp
typedef struct _TextureInfo  TextureInfo;
```
