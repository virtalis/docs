---
sidebar_position: 5
description: 2020.3 to 2024.1
---

# Virtalis Hub

## 2024.1

*Released 2024-03-08*

#### Added

#### Changed

- REACH-180: Refactored and updated platform web pages

#### Fixed

---

## 2023.2

*Released 2023-10-06*

#### Added

- REACH-54: Support for collaboration session api queueing messages
- REACH-73: New session management system

#### Fixed

- REACH-166: Fix a metanode issue that was causing problems with snapshots

---

## 2023.1

*Released 2023-02-28*

#### Fixed

- PLAT-1216: Job status no longer stuck in "Cancellation Requested" status in some edge cases
- PLAT-1208: Improved error handling in the Advanced Editor

---

## 2022.4

*Released 2022-10-18*

#### Changed

- PLAT-1218: Improved compatibility with Keycloak 18

#### Fixed

- PLAT-1150: Created recovery operation for retrieving upload folder.
- PLAT-1219: UI improvements for the jobs page

#### Added

- PLAT-1161: New 'cancel job' and 'view visualisation' buttons on the job listing page
- PLAT-1162: Ability to cancel an 'Import' job
- PLAT-1163: Ability to cancel an 'Publish' job
- PLAT-1061: Ability to cancel an 'Translation' job
- PLAT-1046: Deletes temp binary data after use.

---

## 2022.3

*Released 2022-08-05*

#### Changed

- PLAT-1001: Reduced memory usage of the Translator service.

#### Fixed

- PLAT-1033: Rules for scenes with empty Libraries now work in a way that is consistent with scenes that do have Libraries
- PLAT-1067: More efficient node property storage by using the 'string-default-property-values' flag in the translation step
- PLAT-827: Better exception handling when dealing with MySQL errors
- PLAT-1066: Textures in scenes created in Visionary Render 2019.5 have their RGB channels handled correctly
- PLAT-1150: Created recovery operation for retrieving upload folder.

---

## 2022.2

*Released 2022-05-31*

#### Changed

- PLAT-721: Updated docker images to .NET 6.
- PLAT-1057: Changed the IQueueConnection from singleton to scoped, also chaged all services that request an IQueueConnection to also allow a IQueueWriter.
- PLAT-1067: Properties with default values are now removed in the Translation step.

#### Fixed

- PLAT-643: The 'Try It Out' feature on an API's swagger docs page now works when running behind a reverse proxy.
- PLAT-999: Significantly decreased publishing pipeline memory usage, by making it now use a streaming reader/writer instead of loading the whole file into memory.
- PLAT-1000: Significant decrease in memory usage for the import stage
- PLAT-1058: Prevents duplicate versions of a published asset from appearing in rare cases

#### Added

- PLAT-987: v2.1 of the Project Management API now supports API endpoints for the project icon feature. 

---

## 2022.1

*Released 2022-02-25*

#### Changed

- PLAT-618: Add initial support for job cancellation, the job api now has a /cancel endpoint for cancelling jobs.

#### Fixed

- PLAT-1018: Fixes an issue where services would not exit correctly in the case of a configuration error because they were being kept alive by the queue connection.
- PLAT-907: Added local time usage in the job scheduler. 
- PLAT-877: Fix error handling when a request fails in the Artifact Publishing API
- PLAT-976: Fixes import silently failing when selecting a file in a sub-folder as the main file, and a bug when importing where files with the same name but in different folders would cause the upload to hang indefinitely.
- PLAT-954: Fixes unnecessary extra nodes being created in the tree.

#### Added

- PLAT-729: V2 of the project management api now supports pagination for projects. Adds pagination for projects and assets listed in the Hub.

---

## 2021.5

*Released 2021-12-15*

#### Added

- PLAT-889: Adds OAuth2 Client Credentials support for accessing assets from external systems
- PLAT-891: Also adds Bearer Token and Custom Auth Header support
- PLAT-878: Adds support for custom root certificate authorities.
- PLAT-892: Added config to switch on or off the use of HTTP URLs.

#### Changed

- PLAT-893: Enabling and disabling scripts now requires the relevant role as opposed to a group
- PLAT-868: Improves high LODs of some meshes that have undergone Mesh Splitting, specifically for meshes containing duplicate vertices.
- PLAT-908: Changed importing an asset, so you cannot update an imported asset groups.

#### Fixed

- PLAT-711: Fixes a bug in the creoview translator where only the first instance of an instanced part would be imported correctly.
- PLAT-883: Fixes a bug on the import page where asset names with ampersands in asset name cause display problems.
- PLAT-925: Fixes collaborationSessionApi potentially leaking existence of a session a user should not see.
- PLAT-906: Fixes a bug where projects with names containing special characters would fail to publish
- PLAT-891: Fixes startup error messages to do with invalid secrets so that they include much more detail
- PLAT-722: Fixes a bug where the JobStatusList page would time out inapproppriately, particularly on a fresh boot.

---

## 2021.4

*Released 2021-10-29*

#### Added

- PLAT-652: Uses the Amazon S3 SDK instead of the Minio API to interface with minio. This fixes an issue with connections to the binary store being left open.
- PLAT-625: Adds mesh splitting step to the translation service.

#### Changed

- PLAT-781: Dates and times are now shown in the user's local time rather than in UTC

#### Fixed

- PLAT-858: Fixes a bug where cleaning up of Scratch Area at the end of a Project Publish job could fail.
- PLAT-884: Fixes a bug where the allow scripts setting would not be preserved sometimes between project updates.
- PLAT-864: Fixes a bug where failed publishing jobs could be incorrectly marked as complete under certain conditions
- PLAT-808: Fixes a bug in the advanced editor by performing additional security checks when dealing with assets
- PLAT-909: Fixes a bug with the comment system by performing additional security checks when dealing with threads and comments
- PLAT-911: Fixes a possible database injection in the json rules processing.
- PLAT-918: Fixes slow request times in the ImportFolderApi when there are many files in the folder.
- PLAT-711: Fixes a bug in the creoview translator where only the first instance of an instanced part would be imported correctly.
- PLAT-924: Fixes a bug where file with '#' character in them would fail to import.

---

## 2021.3

*Released 2021-09-01*

#### Added

- PLAT-653: Extra configuration options for performance tuning
- PLAT-617: Adds Version information to import jobs on the jobstatus page.
- PLAT-439: Adds option to allow / disallow scripts when creating / editing a project. Option defaults to disallow, permissions required to change to allow.
- PLAT-691: Adds a service for design review comments.
- PLAT-712: Adds product tree api and the backend to save and restore product tree state.
- PLAT-336: Adds a framework for providing graph database migrations
- PLAT-710: Adds a new pipeline runner action that resolves references to other models, for PLM integration.

#### Removed

- PLAT-730: Removed unnecessary ArtifactPublisherService

#### Changed

- PLAT-655: The text in import folder web app was updated and a space was added after certain checkboxes.
- PLAT-510: Modifies JobQueryApi to search jobs by job parameters.
- PLAT-677: Updated the ProjectAuthorWeb to display that no assets have been found in the AssetPicker and Library.
- PLAT-673: Clarified usage of the properites of a import messages.
- PLAT-476: Reduces log spam and cleans up some log messages.
- PLAT-639: Cleaned up ProjectManagementApi.
- PLAT-485: Move duplicated config variables into reusable classes.
- PLAT-506: Updated DAO layer to use RunNewTransactionAsync.
- PLAT-718: Changed Allow Scripts text to indicate when a user does not have permission to change whether scripts are allowed, and hides radiobuttons when this is the case.
- PLAT-511: Updated ProjectManagementApi to return the created and updated date of vrmodels. It also returns the created date of each vrmodel revision.
- PLAT-715: Upgraded React apps to use React 17.
- PLAT-726: Changed asset list to order to A-z by default.
- PLAT-714: Updated TemplateProcessors to use a token to resume the pipelines rather than re-posting queue message.
- PLAT-705: Pipelines now link artifacts to visualisations
- PLAT-493: Moved common API and Services startup code to shared classes.
- PLAT-693: Job Status page now displays the Source Revision of assets if applicable.
- PLAT-768: RunNewTransaction Async call returns the value rather than just changing it inside its body.
- PLAT-807: Job API now supports a callbackURL which returns a success or failure to the importer.
- PLAT-727: Searchbar automatically searches through the projects/library pages after every letter so no longer a need to press the enter button.
- PLAT-509: Adds links from ProjectRevisions to their VRModels to improve search performance
- PLAT-795: Updated Project list so that Projects are only returned when the user has at least one group for the project and each asset in the project. 
- PLAT-854: Improves some error messages to include more of the Exception stack to aid in debugging issues.

#### Fixed

- PLAT-663: Fixes a thread safety issue with the instrumentation code
- PLAT-646: Fixes issue that would result in a upload failing when two files are uploaded at the same time.
- PLAT-676: Fixes alphabetical sorting of Projects / Assets being case sensative.
- PLAT-706: Fixes issue in group configuration for Projects and Assets, where changes from "Me and groups I have invited" to "Only me" would not be made.
- PLAT-724: Fixed issue where version number shows up on projects that have been published when the source data changes.
- PLAT-684: Fixed issue where artifacts would be viewable before they had completed
- PLAT-794: Fixed a problem where projects couldn't be opened after changing their permissions
- PLAT-858: Fixes bug where cleaning up of Scratch Area at the end of a Project Publish job could fail.
- PLAT-825: Fixed header overflow issue on small screens for Library and Projects (Home) pages.

---

## 2021.1

*Released 2021-03-24*

#### Added

- A new changelog web service where you can request this changelog in markdown (`/changelog/markdown`) or html (`/changelog/html`) format
- PLAT-479: Added filtering, pagination and sorting to the 'vrmodels' endpoint of the Project Management API
- PLAT-614: Adds an endpoint to the changelog service /licenses that includes the licensing information for our third party dependencies
- PLAT-611: Additional configuration for serilog can be made in via appsettings.json or via environment variables. See more info [here](https://github.com/serilog/serilog-settings-configuration)
- PLAT-524: Added sort option to the list of projects.
- PLAT-523: Added sort and search option to the list of assets, project and library index views.
- PLAT-630: Added a new rule 'CreateNodeRule' to the JsonTemplateProcessor.
- PLAT-668: The TranslatorServiceConfiguration.Filesources configuration supports a UrlMapping so that if a request is recieved for a certain hostname, the hostname, port and protocol can be replaced with different values.
- PLAT-121: Stores the remote version of scenes coming from external systems

#### Changed

- Ports for API and web app docker images have been changed to 8080 for everything to be more consistent.
- PLAT-604: The changelog will now have ticket numbers for all entries going forward
- PLAT-613: Centralise version numbers
- PLAT-602: add vrtree migration step to the import of vrnative files, which now takes care of both the new uuid generation and the uuid links output
- PLAT-504: The `{id}` versions GET endpoint for the project api now returns a list of revisions with more detail.
- PLAT-549: Treats the rules in the advanced editor as Json rather than a string (no escape characters required any more)
- PLAT-275: Modifies the internal Scratch Management API so its POST endpoint now starts a background job, callers now use a new GET endpoint to get the status.
- PLAT-615: Silences spammy db query logging for APIs.
- PLAT-636: Fixes non-awaiting asynchronous call in the project API.
- PLAT-576: Modifies the internal Import API so its POST endpoint creates a background job, and the state/result of the job is probed from another two GET endpoints.
- PLAT-640: Modifies the internal Artifact Publishing API to be more stable by utilizing the Polling Pattern.
- PLAT-641: Modifies the internal Scratch Management API to utilize the Polling Pattern to remove very long requests.
- PLAT-624: Changes the file upload to use a background job for the final part of the file upload, so the request now responds instantly instead of stalling when the file reaches 100% and potentially timing out
- PLAT-619: Modified were internal http errors occur to parse the http response content and report the error from it.
- PLAT-642: Modifies the internal Scratch Manipulation API to utilize the Polling Pattern to remove very long requests.
- PLAT-195: Makes it easier to trace the progress of a job
- PLAT-657: Improves some error messages that may show in job status page.
- PLAT-664: Removes warnings about insecure AuthUri for internal connections.
- PLAT-658: Added clearer error handling to the translation stage
- PLAT-651: The VRTree cache is now disabled for the translator host (it was unnecessary and causing exceptions when it was full)
- PLAT-683: Changed the background job framework to use an in-memory database
- PLAT-682: The template json is now automatically formatted when editing the template

#### Fixed

- PipelineSchedulerService: Fixes job type when a publish job is scheduled. It is now ProjectPublished instead of DataAdded.
- Assets that are still being imported will no longer be returned by the ProjectManagementAPI, and so the user (and other services) are not aware of their existence until the import has Completed.
- Shared: Job error message would only display a retry error message this is now been fixed to show the actual error message.
- PLAT-607-PipelineRunnerService: Publishing a Project can publish a different revision than expected is now fixed.
- PLAT-607-ProjectManagementApi: Publishing a Project can publish a different revision than expected is now fixed.
- PLAT-626: VRNative files with missing attributes are now importable again (regression caused by PLAT-602 change above)
- PLAT-627: Fixed an obscure issue where libraries being under nodes other than libraries could cause a duplicate attribute error.
- PLAT-603: Fixed issue where once a project rule is created and published, it can't be revoked.
- PLAT-575: Fixes bug where Lum16 DDS files weren't parsed correctly.
- PLAT-635: No longer throws an exception with an empty rule list, which was preventing projects to have extra assets added to them
- PLAT-633: Fixed issue where a job reported as Complete when the publishing a project fails.
- PLAT-634: Removed unneeded 'Select All' checkbox for the asset picker for a project.
- PLAT-582: Cleaned up IVRModelService to handle exceptions appropriately.
- PLAT-644: Updated try catch blocks in BackendStorage.
- PLAT-631: Fixes issue importing any file with a VR_VEC_STRING property
- PLAT-637: Fixed Job Status being reported as "Ready To Import" when it is already Importing.
- PLAT-621: TranslatorService was using twice the amount of memory it needed to use
- PLAT-620: Fixed Job Status freezing for large jobs.
- PLAT-650: Fixed issue were the projects head revision would be missing for auto projects.
- PLAT-507: Updated Project API to be more robust when handling exceptions.
- PLAT-656: Files that fail to load due to a vrtree error when importing (e.g. incomplete files) now report the exception information rather than just failing with an ambiguous error message
- PLAT-662: Fixes potential connection issues with some services by making hangfire worker thread counts configurable.
- PLAT-655: Error messages related to /health endpoints are no longer filtered out
- PLAT-660: Translating files in subdirectories in the import folder works instead of producing a directory error
- PLAT-667: Fixes the Import wizard presenting the data for a previous import
- PLAT-659: Fixes issue that could result in duplicates of a project showing up in the Project list.
- PLAT-671: Fixes bug that caused a Publish Project job to be stuck as In Progress when it fails to apply a rule.
- PLAT-670: Fixes bug that caused a ChangePropertyRule to fail if the pattern had a backslash in a name.
- PLAT-674: Fixes layout issues on the project and asset list pages when viewing on lower resolution screens
- PLAT-685: Fixes bug preventing the updating of a Project with no rules.
- PLAT-688: Fixes schedule project publishes hanging indefinitely.
- PLAT-672: Add validation to CreateNodeRule so that it can't create nodes with metanode types that don't exist
- PLAT-672: Fix a bug where ChangeNodeRule couldn't be used on unversioned metanodes
- PLAT-672: Update the translator host to add all core metanodes to a model during the translation stage so they're accessible from rules

---

## 2020.3

*Released 2020-11-13*

#### Added

- ScratchManagementApi: Initial version of this service.
- JsonTemplateProcessor: Initial version of this service.
- ScratchManipulationApi: Initial version of this service.
- ArtifactAccessApi: The artifact endpoints are now authorized and have group permissions that can be specified to limit what users have access
- CollaborationSessionApi: The session endpoints are now authorized and have group permissions that can be specified to limit what users have access
- ProjectManagementApi: The projects, models, and templates endpoints are now authorized and have group permissions that can be specified to limit what users have access
- ProjectManagementApi: When retrieving project data extra data is available; a publish trigger, the hour to publish if set to a daily trigger, a list of groups and a list of actions.
- ProjectManagementApi: When retrieving project information extra data is available; whether or not the project is a draft.
- ProjectManagementApi: The publish endpoint will now set the draft status of a project to false.
- ProjectManagementApi: New endpoint available to trigger a project to be built: `POST /projects/{id}/build`
- ProjectManagementApi: New endpoint available to delete a project: `DELETE /projects/{id}`
- JobQueryApi: The job endpoints are now authorized and have group permissions that can be specified to limit what users have access
- ImportFolderWeb: Adds support for chunked uploads and upload resuming

#### Changed

- ImportFolderWeb: Files are now appended when dragging more than one onto the drop zone
- ImportFolderWeb: The paths of files are now shown before upload in the case of folder upload, rather than just the filenames
- PipelineSchedulerService: Renamed service from Artifact Task Generator to Pipeline Scheduler Service
- PipelineSchedulerService: Runs a Job every hour to check if any Projects are set to be run on that hour, if so generates a message for the Pipeline Runner Service.
- PipelineSchedulerService: Creates a job using the Job Api when scheduling a new run of a Project pipeline.
- ArtifactAccessApi: For an empty artifact list, the api will now return an empty list `[]` instead of a `404` error
- ProjectManagementApi: ProjectManagementApiConfiguration updated to have: JobApiAddress, QueueAddress, QueueVHost, QueueName, QueueTimeout

#### Fixed

- TranslatorService: Fixes an error when attempting to download multiple files during translation.
- TranslatorService: Failed translation attempts will now be retried
- TranslatorService: AverageColour for textures used prior to loading texture is now in the correct ARGB format
- CreoviewTranslator: Fixed various issues with Creoview/Windchill file import, including:
  - Mixed case filenames and filenames containing spaces are now supported
  - PVZ files with nested directories are now supported
  - Invalid characters in the tree data in various cases is now fixed
